//
//  HowToUserRestManager.swift
//  GIFT_APP
//
//  Created by Alguz on 12/12/19.
//  Copyright © 2019 Leo Suzin. All rights reserved.
//

import Foundation
class HowToUserRestManager {
    func test() {
        //        let rest = RestManager()
        //        rest.httpBodyParameters.add(value: name, forKey: "name")
        //        rest.httpBodyParameters.add(value: content, forKey: "content")
        //
        //        var fileInfos: [RestManager.FileInfo] = []
        //
        //        for attatch in attaches {
        //            let fileInfo = RestManager.FileInfo(withFileData: attatch.data, filename: attatch.fullName, name: "postImage", mimetype: attatch.mimeType)
        //            fileInfos.append(fileInfo)
        //        }
        //
        //
        //        rest.upload(files: fileInfos, toURL: URL(string: Setting.serverApiURL + "/post/create"), withHttpMethod: .post) { (results, failedFilesList) in
        //            if let error = results.error {
        //                print(error)
        //            }
        //
        //            if let data = results.data {
        //                if let toDictionary = try? JSONSerialization.jsonObject(with: data, options: .mutableContainers) {
        //                    print(toDictionary)
        //                }
        //            }
        //
        //            if let failedFiles = failedFilesList {
        //                for file in failedFiles {
        //                    print(file)
        //                }
        //            }
        //        }
                
                
                
                
        //        let data = [
        //            "name": name,
        //            "content": content
        //        ]
        //        HttpRequest.POST(url: Setting.serverApiURL + "/post/create", data: data, onSuccess: { (data) in
        //
        //        }) { (error) in
        //
        //        }
    }
}
